{
  "name": "fb-ct-node",
  "version": "1.0.0",
  "description": "To start checkout the repo in local follow below steps-  1. add CT connection details in .env file 2. run npm init from the project folder 3. run the node server using npm start 4. Server should start in 3600 port 5. Use below Urls to test the order and customer service -",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@commercetools/platform-sdk": "^2.6.0",
    "@commercetools/sdk-client-v2": "^1.3.0",
    "body-parser": "^1.20.2",
    "chalk-animation": "^1.6.0",
    "cli-highlight": "^2.1.11",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "node-fetch": "2.0"
  }
}
